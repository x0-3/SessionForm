{% extends 'base.html.twig' %}

{% block title %}Hello StagiaireController!{% endblock %}

{% block body %}

<section class="ml-9">

    <h1 class="text-center text-2xl font-semibold my-9">List of interns</h1>

    <div class="flex my-9" id="add_button">
        <a class="p-1 w-20 rounded-md text-center mb-2.5" href="{{path('add_stagiaire')}}">add</a>

    </div>


    {% for stagiaire in stagiaires %}

        <a href="{{path('detail_stagiaire', {'id': stagiaire.id})}}">{{ stagiaire.fullName }} </a> <br>

    {% endfor %}

</section>
{% endblock %}